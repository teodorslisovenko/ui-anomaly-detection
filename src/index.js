import React from 'react';
import ReactDOM from 'react-dom';
import StyledDropzone from './App';

ReactDOM.render(
  <React.StrictMode>
    <StyledDropzone />
  </React.StrictMode>,
  document.getElementById('root')
);
